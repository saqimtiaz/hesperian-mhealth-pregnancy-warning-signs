Various scripts for rsynicing the website, uploading to TestFlight, etc.

Note that they won't actually be checked in to git, since they may contain private-ish info. They should exist somewhere in the local path.
1) hm-site-rsync.sh - upload the www directory to a website.
